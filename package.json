{
  "name": "moviecast-api",
  "version": "0.0.1",
  "description": "The Backend API Server of MovieCast",
  "main": "index.js",
  "scripts": {
    "build": "lerna run --stream build",
    "dev": "lerna run --stream build && lerna run --stream --scope @moviecast/api-server dev",
    "api": "lerna run --stream --scope @moviecast/api-server dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MovieCast/api.git"
  },
  "private": true,
  "author": "Dizzy <itsdizzy40@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/MovieCast/api/issues"
  },
  "homepage": "https://github.com/MovieCast/api#readme",
  "dependencies": {
    "lerna": "^2.11.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^7.2.1",
    "@commitlint/config-conventional": "^7.1.2",
    "@commitlint/config-lerna-scopes": "^7.2.1",
    "@types/mongoose": "^5.2.22",
    "@types/node": "^10.12.1",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "husky": "^1.1.2",
    "ts-node": "^7.0.1",
    "typescript": "^3.1.4"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "workspaces": [
    "packages/*"
  ]
}
